<?xml version="1.0" encoding="UTF-8"?>
<odoo>
       <!-- Form View -->
    <record id="view_medicament_structure_form" model="ir.ui.view">
        <field name="name">medicament.structure.form</field>
        <field name="model">medicament.structure</field>
        <field name="arch" type="xml">
            <form string="medicament Form">
                <group>
                    <group>
                        <field name="category_name"/>                     
                    </group>
                </group>
            </form>
        </field>
    </record>

        <!-- list view -->
    <record id="view_medicament_structure_list" model="ir.ui.view">
        <field name="name">medicament.structure.list</field>
        <field name="model">medicament.structure</field>
        <field name="arch" type="xml">
            <list>
                <field name="category_name"/>
            </list>
        </field>
    </record>

        <!-- action view -->
     <record id="action_medicament_structure" model="ir.actions.act_window">
        <field name="name">medicament.structure</field>
        <field name="res_model">medicament.structure</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>